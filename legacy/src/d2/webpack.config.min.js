var webpack = require('webpack');
var config = require('./webpack.config');

config.output = Object.assign({}, config.output, {
    filename: "d2-browser.min.js",
});

config.plugins.push(
    new webpack.optimize.UglifyJsPlugin({
        compressor: {
            warnings: true
        }
    })
);

module.exports = config;
