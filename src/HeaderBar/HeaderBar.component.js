import React from 'react';
import classes from 'classnames';

const HeaderBar = React.createClass({
    render() {
        const classList = classes('header-bar');

        return (
            <div className={classList}>

            </div>
        );
    },
});

export default HeaderBar;
