import { featureTypeOverride } from './program';

export default new Map([
    [
        'featureType',
        {
            fieldOptions: {
                options: featureTypeOverride,
            },
        },
    ],
]);
