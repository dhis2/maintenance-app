import React from 'react';
import withStateFrom from '../withStateFrom';

describe('withStateFrom', () => {
    it('should be a function', () => {
        expect(typeof withStateFrom).toBe('function');
    });
});
